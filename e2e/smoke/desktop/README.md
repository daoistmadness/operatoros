Desktop smoke is intentionally deferred. Tauri MCP is not installed or usable without application-code changes.

Expected summary state until prerequisites are separately approved: `BLOCKED_BY_EXISTING_INFRASTRUCTURE`.

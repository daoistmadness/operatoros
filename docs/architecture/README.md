# Architecture

The current application architecture is the Phase 14 monorepo (apps/api, apps/web, packages/*) on the local Bun + Elysia + SQLite runtime.

Architecture decision records remain in [`docs/adr`](../adr/) and [`docs/architecture/decisions`](decisions/).

name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - master
      - develop

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: backend
    env:
      PYTHONPATH: src
      DATABASE_URL: sqlite:///./ci-test.db
      ENABLE_DESTRUCTIVE_OPERATIONS: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt
      - run: python -m pip install -r requirements.txt
      - run: python -c "from src.main import app; assert app is not None; routes = {route.path for route in app.routes}; assert '/system/clear-data' in routes; assert '/system/health' in routes"
      - run: python -m pytest -q

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run build

  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Compose configuration
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: development-only-change-me
          POSTGRES_DB: absensi
          POSTGRES_HOST: db
          POSTGRES_PORT: "5432"
          REACT_APP_API_URL: /api
        run: docker compose config

  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate relative Markdown links
        run: python .github/scripts/check_markdown_links.py
